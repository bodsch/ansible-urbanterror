#!/bin/bash
cat <<EOF
{
  "current_version": "{{ urbanterror_current_version }}",
  "download_server": "{{ urbanterror_download_server }}",
  "game_engine": "{{ urbanterror_game_engine }}",
  "ask_before_update": "{{ urbanterror_ask_before_updating }}"
}
EOF
